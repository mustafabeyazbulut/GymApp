---
name: project-member-experience-status
description: GymApp Flutter mobile — Member Experience plan (login + 4-tab mock-data app, replacing the Branch proof-of-concept) execution status. Read this first if resuming, more current than project-mobile-foundation-status.md for anything after 2026-09-14's pivot.
metadata:
  type: project
---

# GymApp Mobile — Member Experience Status

**Plan:** `docs/superpowers/plans/2026-09-14-member-experience.md` (17 tasks) — read it in full before resuming. **Spec:** `docs/superpowers/specs/2026-09-14-member-experience-design.md`.

**Why this plan exists (context for a fresh session):** The prior "Mobile Foundation" plan (see [[project-mobile-foundation-status]]) built a technically-proven but product-thin app: architecture + theme + one backend-gated screen (Branch list/create). After seeing it running, the user was unhappy — they expected the real "MAT & MOVE" mockup app, not a Branch admin screen. User said explicitly: "ben senden böyle bir şey istemedim, uygulamayı inşa etmeni istiyorum" (I didn't ask for this, I want you to build the app). Through an iterative visual-brainstorming session (browser-based mockup tool, v1→v2→v3 with real user feedback each round — too much icon/color/texture clutter, neon green too harsh, button text too bold, random stock photos looked bad/irrelevant), the team landed on an approved v3 visual design, then the user explicitly said: **delete the Branch feature entirely** ("tasarım o olamaz, sil bence") and **build all 5 mockup screens now with mock/local data**, not gated on real backends.

**Key design decisions locked in (do not re-litigate without the user):**
- `AppColors.primary` changed `#C6FF3D` → `#8BC34A` (neon lime → calmer mid-tone green) — direct user feedback ("çok cırtlak", "göz yoruyor"), not an accident.
- Branch feature fully deleted (Task 1) — not archived, not reused. Where "şube/admin yönetimi" eventually lands is an explicitly open, deferred question per the user ("önce ana tasarımı çıkaralım daha sonra onu nereye koyacağımıza karar veririz").
- 5 screens, mock/local data via `Fake...Repository` per feature (`auth`, `home`, `classes`, `progress`, `membership`), same `domain/data/presentation` pattern the Branch feature proved — just without a DTO/JSON layer, since there's no network involved.
- No real auth, no persistence — app always starts at `/login`, any non-empty credentials succeed.
- `StatefulShellRoute.indexedStack` (go_router) for the 4-tab shell; router rebuilds via `ref.watch(authStateProvider)` inside `@riverpod GoRouter appRouter(...)` rather than `refreshListenable` machinery.

**Environment/tooling notes carried over from the Mobile Foundation plan (still true):** PowerShell tool required for all `flutter`/`dart` commands (Bash/Git Bash can't invoke the CLI correctly here); `AsyncValue.copyWithPrevious` is `@internal`, never use it — use a private cache field instead if "preserve last good value across a failed reload" is needed; `NavigationBar` requires ≥2 destinations (not a risk in this plan — 4 real tabs); `build_runner`'s `--delete-conflicting-outputs` flag is deprecated/ignored but harmless, keep passing it; no real browser screenshot tool is available in this environment (no `chromium-cli`, `claude-in-chrome` not connected) — Task 17's manual smoke test relies on log/analyze-based verification, not visual screenshots, and that's an accepted limitation, not a shortcut to flag.

## Task checklist

- [x] Task 1 — Remove the Branch Feature — Last commit: `e180a49` "Remove Branch feature and replace router with temporary placeholder" (spec ✅, code-quality ✅ "Ready to merge: Yes"). Clean, total deletion (14 files) — `lib/features/` no longer exists at all. `lib/core/widgets/app_shell.dart` is now a temporary orphan (nothing imports it) — this is correct/expected, Task 15 rewrites it, not a bug to fix early. `app_router.dart` replaced with the minimal one-route placeholder from the plan, to be replaced for real in Task 16.
- [x] Task 2 — Update `AppColors.primary` to a Calmer Green — Last commit: `65269ae` "Tone down primary accent from neon lime to a calmer green" (spec ✅, code-quality ✅ "Ready to merge: Yes"). Exactly 2 lines changed (`primary`, `successSurface`), nothing else. Contrast vs `onPrimary` recomputed: ~9.25:1, well clear of WCAG AAA even.
- [x] Task 3 — Rewrite Localization (remove Branch keys, add Member Experience keys) — Last commits: `e205a09` "Replace Branch localization keys with Member Experience keys" + `d038d35` "Fix Turkish localization copy inconsistencies" (spec ✅ incl. all placeholder arities cross-checked against Tasks 6/8/14's actual call sites — clean, no mismatch; code-quality ✅ after fix — 4 Turkish copy issues found: `homeCheckInButton` collided verbatim with `loginSubmitButton` ("Giriş Yap" reused for two different actions), `progressTechnique` was plural breaking its sibling gauge labels' singular pattern, `membershipFreezeButton`/`classesReservedButton` had register/vocabulary mismatches vs. their sibling strings — all fixed, plus one free bonus grammar fix (`homeWeeklyAttendanceLabel` missing the "-ki" relativizer)). **If any later task adds a new ARB key, give the Turkish copy the same real-read scrutiny** — this reviewer caught genuine native-speaker-level issues (collision, singular/plural mismatch, register inconsistency) that a structural/arity check alone would have missed entirely.
- [x] Task 4 — `CircularStatGauge` Widget — Last commit: `3b6e5a6` "Add CircularStatGauge widget for progress ring stats" (spec ✅ byte-for-byte match, code-quality ✅ "Ready to merge: Yes" — reviewer worked the actual stroke geometry math and confirmed the ring is mathematically tangent to the widget bounds at every size, no clipping/overflow; `shouldRepaint` proven complete given the painter's field set). Only Minor notes (not fixed): magic-number radians could use `dart:math`'s `pi`, `strokeWidth`/`Paint` objects could be cached/parameterized later if a second call site ever needs it (YAGNI-correct to skip for now, only Task 12 uses this so far).
- [x] Task 5 — Auth Feature (fake repository + auth state) — Last commit: `0e11c17` "Add fake auth repository and auth state provider" (spec ✅, code-quality ✅ "Ready to merge: Yes" — reviewer traced the repo/state split against every downstream consumer in the whole plan file via grep and confirmed it's genuinely load-bearing, not premature abstraction; keep this exact pattern — fake repository simulates the operation with zero app-state coupling, separate provider tracks what the app currently shows — for the other 4 features).
- [x] Task 6 — Login Screen — Last commits: `45dff8f` "Add login screen" + `fdb5660` "Fix login screen keyboard overflow and add missing wordmark" (spec ✅, code-quality ✅ after fix). **Two Important issues found and fixed:** (1) original `Spacer()`-based `Column` could genuinely overflow when the keyboard opens on a smaller phone — fixed by wrapping in `SingleChildScrollView` + replacing `Spacer()` with a proportional `SizedBox(height: MediaQuery...*0.1)` (removing `Spacer`/`Expanded` entirely, since flex widgets don't work inside a scroll view's unbounded height — confirmed no `Spacer`/`Expanded` remains anywhere). (2) the plan's own login screen code (which I wrote) omitted the "MAT & MOVE" wordmark the design spec explicitly calls for — fixed by adding a hardcoded (correctly not-localized, brand names aren't translated) `Text('MAT & MOVE', ...)` under the brand mark. **Gradient/theme-reuse design quality was confirmed genuinely good** (reviewer traced the actual alpha math on the radial glow — reads as a soft glow, not harsh, correctly avoiding the earlier-rejected neon look). **Pattern to reuse for remaining screens:** any screen with a `Form`/text fields needs to be checked for the same keyboard-overflow risk if it uses `Spacer()`/non-scrollable layout — Home/Classes/Progress/Membership screens (Tasks 8/10/12/14) use `ListView` already so should be fine, but worth double-checking in their own code-quality reviews.
- [x] Task 7 — Home Feature (domain + fake repository + provider) — Last commits: `7622bd4` "Add home summary domain, fake repository and provider" + `7c429b1` "Pair weeklyAttendance day labels with values, tighten test assertions" (spec ✅, code-quality ✅ after fix). **Real design bug found and fixed:** `HomeSummary.weeklyAttendance` was `List<bool>` meant to be zipped by position against a separate day-label list Task 8 would define — fragile, could silently desync if a real backend ever returns attendance in a different order. Changed to `List<(String, bool)>` (Dart record pairing label+value directly) — **the plan document itself was updated to match (commit `7c0b82f`)**, including Task 8's not-yet-written HomeScreen code (single loop over the paired record instead of two separately-zipped lists). **If any other task's plan code pairs two same-length lists by position (e.g. a future feature with day/category labels), apply the same paired-record fix proactively rather than waiting for review to catch it.** Also tightened `fake_home_repository_test.dart` to exact-value assertions since the fixture is 100% deterministic — apply the same standard to other Fake repository tests (loose `isNotEmpty`/`greaterThan` assertions are a real gap, not appropriately cautious, when there's no randomness/clock/IO involved).
- [x] Task 8 — Home Screen (Ana Sayfa) — Last commits: `facd1ac` "Add home screen" + `7d7ae5c` "Localize trainer line, wire reservation button to Dersler tab, add card icons" (spec ✅ incl. correct paired-record chart loop verified not reverted, code-quality ✅ after fix — attendance-bar bottom-alignment math verified correct, not just assumed). **3 Important issues found and fixed:** (1) hardcoded unlocalized "Eğitmen:" string — added `homeNextClassTrainerLabel` ARB key; (2) "Rezervasyon" button was a no-op but design spec says it should switch tabs — wired to `StatefulNavigationShell.of(context).goBranch(1)` (this call is currently unreachable/untested since Task 16's real router doesn't exist yet, but compiles correctly and is forward-compatible — confirmed by re-review); (3) two dashboard cards looked flat with no icon accent — added `inventory_2_outlined`/`calendar_month_outlined` icons to their eyebrow rows. Bonus fix: AppBar title now uses `l10n.appTitle` instead of a hardcoded duplicate. **Pattern to watch:** when a plan's inlined button `onPressed: () {}` collapses two buttons to the same "no-op" treatment, check the design spec for whether they're SUPPOSED to differ (one real action, one permanent placeholder) — this exact mistake slipped through plan-authoring once already.
- [x] Task 9 — Classes Feature (domain + fake repository w/ reservation mutation + provider) — Last commit: `a826f7f` "Add class session domain, fake repository with reservation mutation, and provider" (spec ✅, code-quality ✅ "Ready to merge: Yes" — reviewer traced the exact async ordering of `reserveSpot` line-by-line and confirmed it's genuinely race-safe for concurrent taps, not accidentally-safe). **Cross-task gap caught and pre-fixed:** `reserveSpot`'s `StateError` (thrown when a session is already full/reserved) was uncaught anywhere downstream — Task 10's not-yet-written `_reserve` had a `finally` but no `catch`, so a fast double-tap would silently fail with an unhandled Future rejection. Fixed by updating Task 10's plan code (commit `872a166`) to add a `catch` + `SnackBar` before Task 10 was ever implemented — no fix-and-re-review cycle needed since it was caught before implementation. **Pattern to watch:** when a repository method can throw (by design, not by bug), check that the plan's corresponding screen task actually catches it — this is the second time a plan-authoring gap in error-handling wiring was caught by code-quality review (first was Task 8's button-wiring gap).
- [x] Task 10 — Classes Screen (Dersler) — Last commits: implementer's original screen commit + `d4b5326` "Fix chip theme colors and add empty state for classes filter" (spec ✅, code-quality ✅ after fix — re-review independently re-ran `flutter analyze`/`flutter test`, confirmed diff matches plan character-for-character). **Real, SDK-source-verified Critical bug found and fixed:** `ChoiceChip`/`FilterChip` derive their colors from `ColorScheme.secondaryContainer`/`onSecondaryContainer`/`surfaceContainerLow`/`onSurfaceVariant` in Material 3 — NOT from `primary`/`secondary` — and `AppTheme.dark`'s `ColorScheme.dark(...)` (`lib/core/theme/app_theme.dart`) never set those roles, so the category filter chips silently rendered in Flutter's stock purple/mauve defaults instead of the app's brand colors. Fixed with an explicit `chipTheme: ChipThemeData(...)` override (token-based: `AppColors.surface`/`successSurface`/`border`/`onBackground`, `AppTypography`, `AppSpacing.radiusPill`) added to `base.copyWith(...)` — the chosen fix, not patching the missing `ColorScheme` roles directly. **Pattern to watch for any future Material 3 widget added to this app:** always check which `ColorScheme` roles a given widget actually reads from (`ChoiceChip`/`FilterChip` ≠ `secondaryContainer` family, not `primary`/`secondary`) before assuming a custom `ColorScheme.dark(...)` that only sets `primary`/`onPrimary`/`secondary`/`surface`/`error` will style it correctly — it may not, and Flutter fails silently (no error, just wrong-looking stock colors) rather than crashing. **Important issue also found and fixed:** no empty-state UI when the category filter matched zero sessions — added `filtered.isEmpty` branch (icon + new `classesEmptyFilterMessage` ARB key) instead of an empty `ListView`. **Non-issue confirmed by re-review:** no dedicated `ClassesScreen` widget test exists, but this matches the plan's own established convention (no feature screen — home, auth, classes — has a screen-level widget test anywhere in this repo yet; only data-layer/provider tests plus one shared-widget test for `CircularStatGauge`) — not a gap specific to this task.
- [x] Task 11 — Progress Feature (domain + fake repository + family provider) — Last commits: `9dc0bee` "Add progress summary domain, fake repository and family provider" + `48b470a` "Assert exact fixture values in fake progress repository test" (spec ✅ byte-for-byte match to plan, code-quality ✅ after fix — re-review independently confirmed all 9 fields checked for both categories, tests/analyze clean). **Only Important issue found and fixed:** the repository test on the fully-deterministic `_bjj`/`_fitness` fixtures only loosely checked title-inequality + a range check, leaving `classesThisMonth`/`attendanceValue`/`conditionValue`/`trainerNoteText`/`trainerNoteAuthor`/`trainerNoteDate` and the exact `techniqueValue` completely unverified — same loose-assertion-on-deterministic-fixture pattern flagged once before in Task 7; **now flagged twice, so treat "loose assertions on a 100%-static fake-repository fixture" as a standing pattern to check proactively in every remaining Fake repository test (Task 13's membership repository next), not just something a reviewer catches after the fact.** **Architectural note confirmed sound, not duplication:** `ProgressCategory` is deliberately a separate enum from `classes` feature's `ClassCategory` — this repo has no shared-domain package at all, every feature owns its full vocabulary independently; reviewer traced this is consistent with the codebase's actual structure, not just the plan's stated intent.
- [x] Task 12 — Progress Screen (Gelişimim) — Last commits: `7d02fd6` "Add progress screen" + `afcf019` "Fix progress screen tap target and align category tabs with chip design" (spec ✅ byte-for-byte, code-quality ✅ after fix — re-review confirmed the fix uses the actual shared tokens, not hardcoded look-alikes). Correctness (family-provider rebuild on tab switch, scoped `ref.invalidate` retry) was confirmed correct on the first pass — only design-consistency issues needed a fix. **Two Important design issues found and fixed:** (1) `_CategoryTab`'s tap target was ~34dp (below the app's 48dp `AppSpacing.minTapTarget` standard every button uses) — wrapped in a `ConstrainedBox(minHeight: AppSpacing.minTapTarget)`; (2) the BJJ/Fitness category selector here used a completely different visual language (solid-fill rectangular blocks) than the identical BJJ/Fitness taxonomy already rendered as `ChoiceChip` pills in `classes_screen.dart` — restyled to reuse the same tokens as the Task 10 `chipTheme` (`AppColors.successSurface` selected bg, `AppColors.primary` selected text, `AppSpacing.radiusPill` shape) while keeping its own two-way-tab widget (not literally a `ChoiceChip`, since the interaction semantics differ from Classes' filter chips — but now reads as the same design language). **Pattern to watch across remaining screens:** whenever a new screen reintroduces a UI concept that already exists elsewhere in the app (a category/filter selector, a status pill, a stat card), check it against the earlier screen's actual token usage, not just "does it look plausible in isolation" — this is the second time a screen shipped a visually-plausible-but-inconsistent-with-a-sibling-screen control (first was Task 9→10's cross-task error-handling gap, different flavor of the same "check siblings" discipline).
- [x] Task 13 — Membership Feature (domain + fake repository w/ freeze mutation + provider) — Last commit: `5a33222` "Add membership domain, fake repository with freeze mutation, and provider" (spec ✅ incl. exact-value fixture assertions — plan was proactively strengthened before implementation, see below — code-quality ✅ "Ready to merge: Yes", no fix cycle needed). **Proactive plan fix paid off:** since Tasks 7 and 11 both independently flagged "loose assertions on a 100%-deterministic fixture," the plan's Task 13 Step 1 test was rewritten (commit `9480d5e`) to assert every field exactly *before* the implementer ever touched it — this is the first of the 4 fake-repository features to pass code-quality review with zero fix-and-re-review cycles. **This pattern (loose-fixture-assertions) should now be considered closed/solved for this plan** — no more repository tasks remain after this one, so no further proactive plan edits needed for it. **Correctness confirmed:** `requestFreeze`-then-refresh ordering in `MembershipProvider` is structurally identical to `ClassList.reserveSpot`'s mutate-then-`AsyncValue.guard`-refresh pattern (Task 9) — verified byte-for-byte comparable, not just superficially similar. `copyWith({MembershipStatus? status})`'s narrow single-field scope was explicitly judged YAGNI-correct (mirrors `ClassSession.copyWith`'s equally narrow scope), not a design smell — Dart's named-param copyWith is additively extensible so there's no deferred migration cost. **Minor notes only (not fixed, low priority):** `getMembership()` returns `_summary` directly rather than defensively copying (safe today only because `paymentHistory` stays const through every mutation — would silently stop being safe if a future change ever built `_summary` from a mutable list); `requestFreeze()` has no "already frozen" guard unlike `reserveSpot`'s full/already-reserved validation (asymmetry judged acceptable given current scope, not necessarily an oversight).
- [x] Task 14 — Membership Screen (Üyeliğim) — Last commits: `90752fa` "Add membership screen" + `98c3e10` "Show frozen-state label on membership freeze button" (spec ✅ byte-for-byte, code-quality ✅ after fix). This is the last of the 5 mock-data content screens. **One Important UX issue found and fixed:** once frozen, the freeze button disabled but kept showing the original "Üyeliği Dondur" CTA text with no visible confirmation the freeze took — inconsistent with `classes_screen.dart`'s already-approved pattern of swapping button labels to name the current state once reached (e.g. "Yeriniz Ayrıldı"). Fixed with a new `membershipFrozenButton` ARB key + status-branched label, following the same pattern. Double-tap protection, spinner color/contrast choice (correctly `onBackground` for an outlined button vs. `onPrimary` for filled), and const/token usage were all confirmed correct on the first pass — no other fixes needed. **Real architectural finding, deliberately NOT acted on (documented, not a bug):** reviewer noted this app now has 3 independently-implemented "pill/badge" widgets (`home_screen.dart`'s `_Pill`, `classes_screen.dart`'s inline badge, this screen's `_StatusPill`) with already-visible styling drift (`classes_screen.dart`'s badge is missing `fontWeight: w600` that the other two apply). A shared `lib/core/widgets/status_pill.dart` would be the correct fix, but was deliberately deferred — this is the last new content screen in the plan (only Task 15 AppShell, Task 16 Router, Task 17 smoke test remain, none of which add new pill-style UI), so the extraction has no further screens left to benefit it and was judged not worth the churn/re-review cost at this point. **If any future screen work resumes on this app, do this extraction first, and also fix the missing `fontWeight: w600` on `classes_screen.dart`'s badge while doing so.**
- [ ] Task 15 — Real `AppShell` (4-tab floating nav bar)
- [ ] Task 16 — Real Router (`/login` + 4-branch `StatefulShellRoute`)
- [ ] Task 17 — Manual End-to-End Smoke Test

## Next action if resuming

1. Read this file, then `docs/superpowers/plans/2026-09-14-member-experience.md` in full.
2. Run `git log --oneline` in `C:\Users\MBEYAZBULUT\Documents\GitHub\GymApp` to confirm which tasks actually landed — trust git over this file if they disagree.
3. Continue subagent-driven-development (implementer → spec reviewer → code-quality reviewer, fix-and-re-review loop on any issues) from the first unchecked task above.
4. Update this checklist + commit SHA after every task's code-quality review passes.
5. UI tasks (6, 8, 10, 12, 14, 15) must also be checked against `.claude/memory/feedback-design-quality-bar.md` in code-quality review, not just functional correctness — the user has given very specific, demanding visual feedback multiple times this project (nav bar quality, button font weight, accent color harshness) and expects that same bar met without being asked again.
