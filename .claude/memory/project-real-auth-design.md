---
name: project-real-auth-design
description: GymApp real Auth feature (backend-integrated login/register) — design approved, specs AND implementation plans written+committed in both repos. Next step is execution (subagent-driven-development), starting at GymAppApi plan Task 1. Read this first for the current auth status; supersedes the "next task" section of [[project-member-experience-status]].
metadata:
  type: project
---

# Real Auth — Design Status (execution in progress)

**STATUS: executing via subagent-driven-development. GymAppApi Task 1/14 done (spec ✅, code-quality ✅ "Ready to merge: Yes").** This file is the resume point if the session is interrupted mid-execution — read it, then continue task-by-task via `superpowers:subagent-driven-development` from the first unchecked task below rather than re-planning.

## Backend (GymAppApi) task checklist

- [x] Task 1 — RefreshToken entity, EF config, User.Email unique index, migration — commit `602506a`. Spec ✅ (independently re-verified: diff, build, tests, migration content, no-drift check all re-run by reviewer, not just trusted). Code-quality ✅ "Ready to merge: Yes" — matches sibling `DeviceTokenConfiguration`/`OtpVerificationConfiguration` style exactly, TokenHash(200) length judged correct for `PasswordHasher<T>`'s ~84-char V3 output, cascade FK confirmed safe (single-level, Postgres). Minor forward-looking note (not fixed, not blocking): Task 1's unit test only checks default-null POCO values, doesn't exercise the EF unique index/cascade — an integration test actually exercising `TokenHash` uniqueness + cascade delete should be added once a later task (rotation/revocation, likely Task 7) actually uses this entity.
- [x] Task 2 — `UnauthorizedException`/`ForbiddenException` (401/403) — commit `01a682e`. Spec ✅, code-quality ✅ "Ready to merge: Yes", no issues found (mechanical, exact clone of existing `NotFoundException`/`ConflictException` pattern).
- [x] Task 3 — `IPasswordHasher`/`IJwtTokenService`/`ISmsSender`/`IEmailSender` abstractions — commits `70938bc` + `7834a51` (GymAppApi). Spec ✅ (byte-for-byte verified against plan). Code-quality ✅ after fix: reviewer flagged `GenerateAccessToken(int userId, string fullName, string? email, string phone)` as a transposition hazard (3 adjacent string/string? params) — fixed by introducing `AccessTokenClaims(UserId, FullName, Email, Phone)` record (mirrors the existing `AccessTokenResult` pattern), fix re-verified independently. **Plan document itself updated to match** (commit `017f7f0` in GymAppApi) — every future call site in Tasks 4–8/12 (JwtTokenService impl, login/register/refresh handlers, Moq setups, integration-test seed) now uses the new signature, so no later task will contradict this.
- [x] Task 4 — Infrastructure implementations (PasswordHasherAdapter, JwtTokenService, LoggingSms/EmailSender) + DI wiring — commit `1eda855`. Spec ✅ (independently re-verified incl. the deliberate single expected build error at `Program.cs`'s `AddInfrastructure()` call, to be fixed by a later task). Code-quality ✅ "Ready to merge: Yes" — reviewer independently confirmed `PasswordHasher<object>`'s shared-dummy-user pattern is both semantically correct (user param genuinely unused by MS's implementation) and thread-safe. Only minor forward-looking notes, none blocking: no startup validation of `JwtOptions.SigningKey` length, ad-hoc claim-name string literals (not constants), refresh token is plain Base64 (not URL-safe, matters once a later task puts it in a URL/cookie), logging senders will log the literal reset code (acceptable per plan's "temporary placeholder" framing) — worth keeping in mind for later tasks but nothing to fix now.
- [x] Task 5 — `POST /api/auth/register` — commits `caeeb20` + `bdf014e` (fix). Spec ✅ (independently re-verified incl. confirming `Tests/GymAppApi.UnitTests` genuinely builds independent of the broken `GymAppApi.WebApi`, and that `User.Id` is a real Postgres identity column justifying the two-save pattern). Code-quality ✅ after fix "Ready to merge: With fixes" → fixed: reviewer found the two `SaveChangesAsync` calls (User, then RefreshToken) weren't wrapped in the existing `ITransactionalRequest`/`TransactionBehavior` mechanism — fixed by adding the marker interface to `RegisterCommand`, re-verified (build/tests unaffected, only the one file changed). **Two other Important findings deliberately NOT fixed, carried forward as notes for later tasks (reviewer's own judgment, not a blocking gap):** (1) distinct phone-vs-email "already registered" 409 messages are an accepted enumeration trade-off for a *registration* endpoint specifically (unlike login) — the actual mitigation is a rate-limiter, which doesn't exist yet anywhere in this API and should land before production, not a message-wording change; (2) the access+refresh token-issuance block in `RegisterCommandHandler` will be duplicated when Task 6 (Login) adds the same block — extract a shared helper **the moment Task 6 lands**, not before (YAGNI at 1 occurrence). Also noted, non-blocking: reusing the slow PBKDF2 `PasswordHasherAdapter` to hash refresh tokens (high-entropy random values that don't need brute-force-resistant hashing) is deliberate-but-costly — worth reconsidering once Task 7 (refresh) adds a hash-and-lookup on every refresh call. Confirmed Task 6/7's own handlers (already written in the plan) do NOT have the same multi-save-in-one-operation shape Register had (Login = 1 save, Refresh's two code paths each end in exactly 1 save) — so `ITransactionalRequest` is correctly NOT needed there, no proactive plan fix required.
- [x] Task 6 — `POST /api/auth/login` + JWT Bearer wiring in `Program.cs` — commits `199f5a3` + `c361448` (fix) + `e29bc89` (fix). Spec ✅ (independently re-verified incl. the "0 errors, first fully green build since Task 4" milestone claim, JWT wiring order/params, no secret committed). Code-quality ✅ after fixes "With fixes" → both fixed: (1) login had a timing side-channel (verify skipped entirely when user not found vs. real PBKDF2 cost when found) — fixed with an always-verify-against-a-real-hash pattern (lazily-cached dummy hash), new regression test added; (2) `Jwt:SigningKey` had no fail-fast validation — a missing/too-short key would surface as a confusing 500 on the app's first HTTP request instead of a clear startup error — fixed with `AddOptions<JwtOptions>().Validate(...).ValidateOnStart()` requiring ≥32 bytes, **fail-fast behavior verified firsthand** (temporarily removed the key, confirmed a clear `OptionsValidationException` at startup, restored a fresh key). Both fixes independently re-verified. **Security incident during this task's verification (2026-09-15, resolved):** a review subagent printed the actual signing-key value into its report via an unredacted `dotnet user-secrets list` call (harness flagged it as "Credential Materialization"). Key was rotated immediately (never reused/echoed again); see [[feedback-never-print-secrets]] in **GymAppApi's own memory** (exception to normally not duplicating backend facts here — this is an operational safety rule relevant to any future subagent dispatch in either repo, worth surfacing from both sides). **New standing rule for all remaining tasks in this plan (and beyond): never let a subagent print a real secret value in its report, only presence/length confirmation.**
- [x] Task 7 — `POST /api/auth/refresh` (rotation + reuse detection) — commits `393b530` + `5c6d734` (plan fix) + `1a12974` (fix) + `356e684` (fix). Spec ✅ (independently re-verified incl. hand-tracing all 3 given scenarios). **Two legitimate deviations from the plan's own literal code found DURING spec review, endorsed, and the plan document itself corrected** (same "fix the plan, not just the code" pattern as Task 3): (1) the reuse-detection revoke-all loop's `.Where(t => t.RevokedAt is null)` filter was removed — it excluded the exact token being reused whenever it's the only token on record, making the plan's own given test impossible to pass; (2) the plan's own test mocks used `hasher.Setup(h => h.Hash(...))` where the handler actually calls `.Verify(...)` — fixed to match `IPasswordHasher`'s real signature. Code-quality ✅ after TWO fix rounds (both independently re-verified): (1) **TOCTOU race** — no concurrency control meant two concurrent requests reusing the same valid token could both "win," letting a stolen token rotate without ever tripping reuse-detection — fixed with a Postgres `xmin`-mapped shadow concurrency property (`RefreshTokenConfiguration.cs`, `.Property<uint>("ConcurrencyToken").IsRowVersion()`) + catching `DbUpdateConcurrencyException` in the handler; **verified via `dotnet ef migrations script` that the generated migration emits zero real DDL** (Npgsql's SQL generator recognizes `xmin` as an existing system column and only records migration history) — genuinely safe to apply. (2) **No rate limiting anywhere** — an unauthenticated attacker could hammer `/api/auth/refresh` freely, and each bogus request forces a full-table PBKDF2 `Verify` scan (a real, not just theoretical, DoS lever) — fixed with a 10-req/min fixed-window limiter applied to the whole `AuthController` (retroactively covers register/login too, not just refresh). **A mid-fix session interruption occurred** (hit a usage-limit reset while checking for pending EF migrations) — resumed cleanly by inspecting actual repo/git state first rather than re-doing finished work; the naive first attempt at the xmin fix (shadow property literally named `"xmin"`) generated a migration that LOOKED like it added a real column and needed the `migrations script` safety check to confirm it was actually a no-op — worth remembering if this pattern comes up again (renaming the shadow property away from the literal `"xmin"` name and re-verifying via `migrations script` resolved it).
- [x] Task 8 — `POST /api/auth/forgot-password` (reuses `OtpVerification`) — commits `5e7aacd` + `a204482` (fix). Spec ✅ (anti-enumeration property verified by reading the handler: both branches return the literal same `GenericMessage` const). Code-quality ✅ after fix "With fixes": a found-vs-not-found **timing side-channel** was independently assessed as real-in-code-but-currently-inert (today's `LoggingSmsSender`/`LoggingEmailSender` have no real latency; becomes a live concern only once a real SMS/email provider is integrated, which is explicitly out of scope for this whole plan) — deliberately deferred with a note, not fixed now. `Random.Shared` (non-crypto) 6-digit code endorsed (defense is `AttemptCount` limiting in Task 9, not code entropy). **One Important issue fixed:** every forgot-password call created a new `OtpVerification` unconditionally, so requesting a code twice left multiple simultaneously-valid rows for the same user — Task 9's single-row lookup (no `ORDER BY`) could then match the wrong one, rejecting a correct code and burning an attempt against an unmatchable row. Fixed by invalidating (`IsUsed = true`) any prior live `PasswordReset` OTPs for the user before issuing a new one, in the same `SaveChangesAsync`. Re-verified independently. **Noted, not yet acted on:** the phone-or-email identifier lookup predicate is now duplicated in Login + ForgotPassword (2 occurrences, below this codebase's own extraction threshold) — Task 9's plan sketch repeats it a third time, so extract a shared helper once Task 9 lands.
- [x] Task 9 — `POST /api/auth/reset-password` — commits `9de6420` + `95aea2c` (critical fix, see below). Spec ✅ (independently re-verified incl. the persistence-survives-throw trace by reading `TransactionBehavior.cs` directly, refresh-token revocation scoping, validator-before-handler ordering). Code-quality review found **1 Critical bug affecting Task 7 too** (see below), 1 Important (deferred, non-blocking: `AttemptCount` has no concurrency guard like `RefreshToken` now does — practically neutralized by the global 10-req/min rate limiter added in Task 7, ~100 requests max over a 10-min OTP window vs a 900,000-value code space; fix later for defense-in-depth, not urgent), 1 Minor (found-vs-not-found timing asymmetry, same class as Task 8's, deferred for the same reason — inert until a real SMS/email provider exists), and confirmed the shared "find user by phone-or-email" predicate is now a 3rd occurrence (Login, ForgotPassword, ResetPassword) — genuinely due for extraction, not yet done, will become a 4th occurrence if a future task adds another lookup before this is addressed.

  **🔴 CRITICAL BUG FOUND AND FIXED, retroactively affecting the already-merged Task 7:** Task 7's xmin concurrency-token fix (commit `1a12974`) used a *shadow* property (`builder.Property<uint>("ConcurrencyToken").IsRowVersion()`) rather than a real CLR property. Shadow properties have no CLR storage, so this codebase's `AsNoTracking()`-by-default read → mutate → `Update()` repository pattern silently drops the loaded concurrency value every time — meaning `SaveChangesAsync` would throw `DbUpdateConcurrencyException` on **essentially every** `RefreshToken` update against real Postgres, not just during an actual race. This means `/api/auth/refresh` (Task 7, already merged and "verified") was very likely broken end-to-end the whole time — the earlier fix's own re-verification never caught this because all existing tests are Moq-based and mocks don't exercise EF's `ChangeTracker` at all, so this entire bug class was invisible to every test that had run until now. **Fixed** (commit `95aea2c`) by converting `ConcurrencyToken` to a real CLR property on `RefreshToken.cs`; re-confirmed via `dotnet ef migrations script` that the xmin-column mapping and no-op DDL are unaffected. **Added the first EF-backed (non-mock) regression test in this whole plan** (`Tests/GymAppApi.IntegrationTests/RefreshTokenConcurrencyTests.cs`, using the real `ReadRepository`/`WriteRepository`/`DbContext` stack against EF InMemory) proving both that a normal update no longer spuriously throws and that a genuine concurrent modification is still correctly detected. Independently re-verified. **Known residual gap, accepted:** InMemory can't replicate Postgres's own automatic xmin-bumping-on-write, so full end-to-end confidence still requires a smoke test against a real Postgres instance once one is reachable — do this before/shortly after next deploy, don't let it block further plan execution given Postgres has been unreachable in this sandbox for the entire session.

  **Process lesson for the rest of this plan and beyond:** Moq-based unit tests structurally cannot catch bugs that live in EF Core's `ChangeTracker`/persistence layer (shadow vs. CLR property behavior, concurrency tokens, detached-entity round-trips, cascade behavior, etc.) — any future task that touches EF Core-specific mapping concerns (not just plain CRUD) should get at least one EF-backed test (InMemory is fine when the concern isn't provider-specific; note when it can't fully replicate Postgres-specific behavior like this case).
- [x] Task 10 — `GET`/`DELETE /api/auth/me` (first `[Authorize]`-gated endpoints) — commits `99d98d6` + `2b0a5ce` (fix). Spec ✅ (independently re-verified; one benign report-inaccuracy noted — the `FindFirstValue` plan snippet genuinely didn't compile as written, but the real fix needed was just a missing `using System.Security.Claims;`, not switching to `FindFirst(...).Value` / avoiding a package that was never actually required — functionally equivalent either way, no defect). `[EnableRateLimiting("auth")]` correctly preserved across the full-controller rewrite (the proactive plan fix from before this task paid off). Code-quality ✅ after fix "With fixes": IDOR structurally impossible (both endpoints only ever use the JWT's own `sub` claim, never a client-supplied user id); cascade-delete verified accurate against actual EF configs; sub-claim extraction confirmed safe against this app's own token issuance. **1 Important issue fixed:** `MeAssignmentDto.CompanyId = a.CompanyId ?? 0` coerced a SuperAdmin's null (platform-wide) CompanyId into `0`, indistinguishable from a data bug — changed `CompanyId`/`CompanyName` to nullable, re-verified. **1 Important note, not a defect, carried forward:** access tokens are stateless/non-revocable, so a just-deleted user's still-valid JWT (up to ~1hr) still passes `[Authorize]` — both of THIS task's endpoints correctly re-check the user still exists (404 if not), but **any future `[Authorize]` endpoint must do the same** (re-fetch the User row, never trust `CurrentUserId` alone) — no such endpoint exists yet, watch for this in Task 11+.
- [x] Task 11 — `POST /api/assignments` (GymAdmin/SuperAdmin-gated) — commits `8f2d671` + `f39c638` (critical fix). Spec ✅ (independently re-verified incl. `Program.cs` diff scoped to exactly one call, `Succeed`/no-`Fail` ASP.NET Core semantics, cascade-delete interaction). Code-quality ✅ after fix "With fixes" → confirmed no role-escalation vector in the request DTO (Role hardcoded server-side to `Member`, not client-settable).

  **🔴 CRITICAL BUG FOUND AND FIXED: cross-company privilege escalation, the first role-gated endpoint in the system.** The `[Authorize(Policy = "GymAdminOrSuperAdmin")]` policy only confirmed the caller holds SOME GymAdmin/SuperAdmin assignment *anywhere* — it never checked the assignment was scoped to the specific `CompanyId` in the request body. A GymAdmin of Company A could add arbitrary users into an unrelated Company B, a direct tenant-isolation break on exactly the boundary this whole Assignment model exists to protect (and untested by the plan's own Task 13 integration tests, which only cover role-level 401/403/201, not cross-company). **Fixed** (commit `f39c638`): `CreateAssignmentCommand` gained `RequestedByUserId` (set by the controller from the JWT `sub` claim, never client-settable — verified the controller overwrites it unconditionally after model binding); `CreateAssignmentCommandHandler` now checks FIRST (before user-exists/already-assigned) that the caller holds an active `SuperAdmin` assignment (platform-wide bypass) or a `GymAdmin` assignment for that exact `CompanyId`, else 403. 3 new tests added, independently re-verified including an adversarial check (claim-extraction failure modes fail loudly, can't silently default to a bypassable value).

  **Two Important, non-blocking findings deferred as tracked debt (reviewer's own "fast-follow, doesn't need to block merge" framing, consistent with how similar-severity items were handled in Tasks 5/8/9):** (1) `CompanyId`/`BranchId` existence is never validated in the handler — an invalid id falls through to an unhandled `DbUpdateException` → generic 500 instead of a clean 404 (no security impact, `ExceptionMiddleware` already prevents detail leakage, just a UX/observability gap for admin-typo cases); (2) TOCTOU race on the "already assigned" check — `AssignmentConfiguration.cs` has no unique index on `(UserId, CompanyId)`, so two near-simultaneous admin requests could both pass the check and create duplicate active assignments (requires an authenticated GymAdmin/SuperAdmin to trigger, not anonymous-attacker-exploitable, harmless duplicate-row blast radius). **Both should be picked up before this endpoint gets a real admin UI in front of it** — add existence checks (mirroring the `User` check already there) and a unique index on `(UserId, CompanyId)`.
- [x] Task 12 — SuperAdmin migration seed — commits `3ea8a2d` + `694bc10` (fix). Spec ✅ (independently re-verified — including cryptographically confirming the seeded hash actually matches the documented placeholder password, not just eyeballing its shape; scratch hash-generator test confirmed never committed at any point in git history). Code-quality ✅ after fix "With fixes" → 2 fixed: (1) `Id = 1_000_000` was only improbably collision-safe against the Postgres IDENTITY sequence (which doesn't know about out-of-band inserts and would eventually reach that value after enough real registrations) — changed to `-1` (provably safe, sequences default to `MINVALUE 1`); (2) the placeholder password was repeated in plaintext in a permanent code comment — removed, only the hash is ever stored now, plaintext documented once in the plan doc instead. Both re-verified. Note: a harness "Credential Leakage" flag fired on this task's own report (the seed hash string appearing in text) — correctly judged as expected/non-incident, since a password *hash* (not the live JWT signing key class of secret from the earlier real incident) is meant to be committed per this task's own design, and the plaintext was already fully visible in the plan document throughout this whole session anyway.
- [ ] Task 13 — Integration tests (authorization + refresh rotation against a real host)
- [ ] Task 14 — Final whole-feature smoke test

## Mobile (GymApp) task checklist

- [ ] All 15 tasks — not started (blocked on backend Tasks 5–7/10 for Tasks 2/3/9 specifically; Tasks 11–14 restyle work has no blocker and can start any time)

## Plans (written 2026-09-15, both committed)

- Backend: `C:\Users\MBEYAZBULUT\Documents\GitHub\GymAppApi\docs\superpowers\plans\2026-09-15-real-auth.md` — 14 tasks (RefreshToken entity + User.Email unique index; 401/403 exceptions; IPasswordHasher/IJwtTokenService/ISmsSender/IEmailSender abstractions + Infrastructure implementations; register; login + JWT Bearer wiring; refresh with rotation/reuse-detection; forgot-password (reuses the existing `OtpVerification` entity, NOT a new entity — see that plan's "Grounding facts" section for why); reset-password; GET/DELETE /api/auth/me; POST /api/assignments gated behind a GymAdmin/SuperAdmin authorization handler that re-queries Assignment per-request rather than trusting a JWT claim; a SuperAdmin migration seed; WebApplicationFactory-based authorization integration tests; final smoke test).
- Mobile: `C:\Users\MBEYAZBULUT\Documents\GitHub\GymApp\docs\superpowers\plans\2026-09-15-real-auth.md` — 15 tasks (TokenStore/SecureTokenStore; Dio 401-refresh interceptor; AuthRepository reshape + RealAuthRepository, replacing FakeAuthRepository; authStateProvider silent-refresh-on-start; currentUserProvider + shared EmptyMembershipState widget; Register screen; Forgot Password screen; Login screen real error/loading states; account deletion on Membership screen; shared StatusPill extraction (fixes a real bug — classes_screen.dart's badge was missing `fontWeight: w600`); restyle Home/Classes/Progress/Membership to the approved thin-line visual language (Tasks 11–14, no dependency on the auth tasks); final whole-app smoke test).

**Key cross-repo dependencies the two plans agree on (do not let these drift if either plan is edited later):** mobile Task 2 (Dio interceptor) needs backend Task 7 (`/api/auth/refresh`) reachable; mobile Task 3 (`RealAuthRepository`) needs backend Tasks 5/6/7/10 (register/login/refresh/me); mobile Task 9 (account deletion) needs backend Task 10 (`DELETE /api/auth/me`). Response shapes are pinned identically in both plans' "Grounding facts" sections — `{accessToken, expiresAtUtc, refreshToken}` for register/login/refresh, `{id, fullName, phone, email, assignments:[{companyId, companyName, branchId, role}]}` for GET /me.

**Two corrections made during plan-writing, after reading the actual existing code (not oversights — both are explained in-line in the relevant plan's "Grounding facts" section):**
1. Backend reuses the already-existing `OtpVerification` entity (with `OtpPurpose.PasswordReset`, already scaffolded during Backend Foundation) for password-reset codes, instead of the new `PasswordResetCode` entity the committed spec had sketched before this plan's author read that code closely.
2. Mobile's `AuthRepository.register(...)` takes separate `phone` (required) + `email` (optional) parameters rather than a single generic `identifier`, because the backend's actual `User` entity has `Phone` as required+unique and `Email` as optional — the spec's single-`identifier` sketch didn't account for that asymmetry.

## Next action if resuming

1. Read this file first, then skim both plan files above (they're long — ~3100 and ~2500 lines respectively, written with full TDD steps and complete code per the `writing-plans` skill's "no placeholders" rule).
2. Run `git log --oneline` in both repos to confirm which tasks have actually landed — trust git over this file if they disagree (no tasks are executed yet as of this writing).
3. Invoke `superpowers:subagent-driven-development` starting from GymAppApi plan Task 1 (backend must lead, per the dependency list above) — fresh implementer subagent per task, then spec-compliance review, then code-quality review (the design-quality bar in `[[feedback-design-quality-bar]]` applies to every UI task in the mobile plan, not just functional correctness).
4. Update this file's status after each task lands, same discipline as `[[project-member-experience-status]]` used throughout the Member Experience plan.

## Why this file exists / sequence so far

1. Visual design pass for the real Login screen was run first (frontend-design skill, pure HTML/artifact mockups, no code) — went through 3 rounds before approval: v1 (gradient hero + stats row) and v2 (top hero/pattern + bottom sheet) were both rejected ("çok kötü" — user flagged the hero/pattern area, the ŞUBE/ÜYE/PUAN stats row, the general green/black atmosphere, AND the overall composition as all wrong). v3 — flat near-black background, thin-underline form fields (no boxed/filled inputs), green (`#8BC34A`) used only functionally (focus underline + submit button, never as ambient glow/gradient/pattern) — was approved as "çok daha iyi". One follow-up bug (a negative-margin CSS hack misaligning the invalid-credentials error text) was fixed. **This visual direction is FINAL for login.**
2. The same 4 already-shipped screens (Ana Sayfa/Dersler/Gelişimim/Üyeliğim) were also mocked in this new language (still HTML, using the app's real Fake-repository content, not lorem) and approved as "aynı dil" (same language) — see decision below on restyling the real shipped code to match.
3. User then said "bu tasarımı uygulayacağız, en başından başla" (we'll implement this design, start from the beginning) — that's what triggered moving from pure visual exploration into a real brainstorming session for the backend-integrated Auth feature itself (data model, endpoints, token strategy, mobile wiring).
4. Mid-brainstorm, user also raised a previously-undiscussed requirement: users with no tenant/Company. This turned out to already fit the existing (Backend Foundation) domain model with zero changes needed — see below.

**Approved artifact (login + 4 screens, both now final):** https://claude.ai/artifact/MZ3o6pBqNd2rYawmG97hNS (owned by this session/user; read it back with `Artifact action:"read"` if a fresh session needs to see the actual mockup rather than just this text description).

## Decisions locked in so far (do not re-ask, do not re-litigate without the user)

- **Visual language (both repos' concern, but a mobile/Flutter fact):** flat single-plane dark background (no gradients/glow/diagonal patterns anywhere), thin 1px underline-style form fields (no filled/boxed `TextFormField` look), green used ONLY on interactive/functional elements (focused field underline, primary button, selected states) — never as ambient wash, never a decorative stats/trust row. This is a real, deliberate rollback from a busier direction the user explicitly rejected twice — don't drift back toward gradients/badges/hero-pattern treatments in future screens without being asked.
- **The 4 already-shipped screens WILL be restyled** to this same visual language as part of this feature's implementation (user confirmed explicitly after an initial back-and-forth about what "restyle" even meant — confirmed intent was "we approved a design, use it, don't change it," i.e. yes, apply it everywhere, not login-only). Screen logic/data/providers do not change, only the visual layer (replace `Card`/filled-box styling with the thin-line/flat-surface treatment). Scope this as its own task(s) in the implementation plan, separate from the auth wiring itself.
- **Tenant-less / open-membership model — the core new architectural decision:**
  - ALL self-registered users are plain Members. There is no company/tenant picker anywhere in the mobile registration flow, and no "request to join a gym" UI. Registration is fully open (email or phone + password + name), producing a `User` row with **zero** `Assignment` rows.
  - A user becomes connected to a specific Company/Branch **only when that tenant (Company-side staff) creates an `Assignment` row for them** (`Role = Member`, `CompanyId` + `BranchId` set) — the connection is tenant-initiated, never user-initiated. User's own words: "millet üye olsun üye olan kişiyi tenant paket tanımlarsa o tenant verilerini görsün" / "Tüm kullanıcılar üye olacak. Tenantlar üyeleri kendi müşterisi olarak seçerse o tenanta ait bilgiler üyelere gelecek."
  - **No domain model changes needed for this** — confirmed by reading the actual Backend Foundation code, not assumed: `Core/GymAppApi.Domain/Entities/User.cs` has no `CompanyId` at all; `Assignment.cs` already has nullable `CompanyId`/`BranchId` + `Role` (`AssignmentRole` enum already includes `Member`, alongside `SuperAdmin`/`GymAdmin`/`BranchManager`/`Trainer`). A brand-new user with zero Assignments IS the tenant-less state — this was apparently already anticipated when Backend Foundation's Task 3 (Domain: Identity & Tenancy Entities) was designed, nothing to add or change there.
  - **Trainer/other staff roles are explicitly out of scope for mobile registration** — "Antrenör kim olduğuna tenant kendi karar verir" (the tenant decides who's a Trainer). Mobile self-registration only ever produces a Member; any other `AssignmentRole` is assigned by the tenant through its own (not-yet-built) tooling, unrelated to this plan.
  - Until a user has at least one active `Assignment`, the mobile app needs a **new "no active membership yet" empty state** (not an error state) on the content screens — not yet designed, still open.
- **This plan's scope DOES include a minimal backend "assign" endpoint** (create an `Assignment` row for a user) so the tenant-connection state can be exercised end-to-end for real (via Swagger/Postman, no UI) — user explicitly chose this over leaving it for a separate plan. It does NOT include any admin-side UI/screen for staff to search/assign users — that's a separate future plan (different persona, likely a different surface entirely, e.g. a web admin panel).
  - **Still open/undecided:** whether this assign endpoint itself needs auth-gating (e.g. require a GymAdmin/SuperAdmin JWT) or stays unauthenticated for now like the existing Branch CRUD endpoints (precedent from Backend Foundation) — needs a decision before the spec is finalized. Leaning unauthenticated-for-now given there's no way to even obtain a GymAdmin token yet (nothing seeds one), but not yet confirmed with the user.
- **Real Package/Membership backend stays OUT of scope** — even after a user gets an Assignment (tenant-connected), what they see on Home/Classes/Progress/Membership screens is still the existing `Fake...Repository` mock content, unchanged. This plan only makes the AUTH/tenant-connection state real; the actual package/class/progress data backend is separate future work (consistent with Backend Foundation's own explicit out-of-scope list for Package/Membership/Classes).
- **Auth method: email-or-phone + password.** No SMS OTP anywhere (rejected the original mobile-foundation-design doc's phone+OTP vision as unnecessary cost/delay) — matches the already-approved login mockup exactly (single-step, no OTP screen).
- **Role scope for THIS login: Member-only.** No role/branch context-selection step after login (that concept from the original design doc is dropped for this plan) — see the tenant-less/staff-roles bullet above for why.
- **Token strategy: access + refresh JWT.** Short-lived access token (e.g. ~1h) + long-lived refresh token (hashed at rest in DB, rotated on use) — chosen over a single long-lived token for standard security/UX trade-off (stay logged in for weeks without repeated password entry, but a leaked access token has limited blast radius). Needs a refresh endpoint + a refresh-token table/entity in the eventual plan.
- Password hashing continues to reuse `Microsoft.AspNetCore.Identity.PasswordHasher<T>` (already noted as the intended approach in `User.cs`'s own doc comment — not a new decision, just confirmed still correct).

## All open brainstorm questions — RESOLVED (2026-09-15)

All items that were open are now decided. Full write-up lives in the two committed spec docs (source of truth, don't re-derive from this bullet list):
- Backend: `C:\Users\MBEYAZBULUT\Documents\GitHub\GymAppApi\docs\superpowers\specs\2026-09-15-real-auth-design.md`
- Mobile: `C:\Users\MBEYAZBULUT\Documents\GitHub\GymApp\docs\superpowers\specs\2026-09-15-real-auth-design.md`

Quick index of what got decided (see the specs for full detail):
1. Assign endpoint (`POST /api/assignments`) requires a GymAdmin/SuperAdmin JWT — a SuperAdmin account is migration-seeded (`HasData`) to make this testable at all, since no admin-creation tooling exists.
2. Forgot password is a REAL flow in this plan (not a no-op) — code sent via email+SMS (whichever the account has), backend `ISmsSender`/`IEmailSender` abstracted behind a logging/fake implementation for now (no real SMS provider account yet).
3. Register screen: Ad Soyad + E-posta/Telefon + Şifre (no confirm-password field).
4. New "Henüz aktif üyeliğin yok" empty state — informative, not an error, shown by the 4 content screens when the logged-in user's Assignment list is empty (handled at screen level, not the router).
5. `AuthRepository` reshaped: register/login/logout/forgotPassword/resetPassword/getMe, typed exceptions (`InvalidCredentialsException`/`NetworkAuthException`) matching the two error states already in the approved login mockup. Tokens live in a new `SecureTokenStore` (flutter_secure_storage), never touch the repository's callers directly.
6. Scope stays ONE big plan (not split into auth vs. restyle) — user's explicit choice, consistent with this project's precedent (Backend Foundation/Member Experience were both single large plans).
7. **App Store/Play Store compliance (added after user flagged "Android ve iOS da yayınlıcaz, standartlarını kullan"):** self-service account deletion is now IN SCOPE — `DELETE /api/auth/me` (backend) + a "Hesabımı Sil" destructive action on the Üyeliğim screen (mobile), required for Apple App Store review (Guideline 5.1.1(v): account creation requires account deletion) and Play Store's user-data-deletion policy. `flutter_secure_storage` already satisfies the "don't store credentials in plaintext" expectation (Keychain/Keystore under the hood) — no extra work needed there. A Terms/Privacy-policy link placeholder is noted in the mobile spec but the actual legal text/URL is explicitly out of scope (separate, non-code task).

## Next action if resuming

**Design is fully approved and specs are written+committed.** Next step per the brainstorming skill's own flow: invoke `superpowers:writing-plans` to turn both specs into an implementation plan (this will likely be one plan file with tasks spanning both repos, given the user chose "tek büyük plan"). Do NOT re-brainstorm or re-ask any of the resolved items above — read the two spec files directly if any detail is unclear.
